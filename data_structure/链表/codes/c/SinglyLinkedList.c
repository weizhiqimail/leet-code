#include <stdio.h>

// 单个结点
typedef struct Node {
    int data;
    struct Node* next;
}Node;

// 链表的结构
typedef struct SinglyLinkedList {
    // 链表的首结点
    Node* head;
    // 链表的大小（长度）
    int size;
    // 链表的容量
    int capacity;
} SinglyLinkedList;


int f() {

}






