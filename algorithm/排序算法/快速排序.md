


# 5. 快速排序代码示例

- 对 `[64, 34, 25, 12, 22, 11, 90]` 进行排序。

```text
详细排序过程:

快速排序调用：low=0, high=6
分区操作开始：low=0, high=6, 基准值=pivot=90

比较：arr[0] = 64 <= pivot (90) → true
  交换：arr[0] 和 arr[0] → [64,34,25,12,22,11,90]

比较：arr[1] = 34 <= pivot (90) → true
比较：arr[2] = 25 <= pivot (90) → true2,22,11,90]
比较：arr[3] = 12 <= pivot (90) → true2,22,11,90]
比较：arr[4] = 22 <= pivot (90) → true2,22,11,90]
比较：arr[5] = 11 <= pivot (90) → true2,22,11,90]
  交换：arr[5] 和 arr[5] → [64,34,25,12,22,11,90]


放置基准值：交换 arr[6] 和 arr[6] → [64,34,25,12,22,11,90]
分区完成，返回分区索引：6
------------------------------


  快速排序调用：low=0, high=5
  分区操作开始：low=0, high=5, 基准值=pivot=11

  比较：arr[0] = 64 <= pivot (11) → false
  比较：arr[1] = 34 <= pivot (11) → false
  比较：arr[2] = 25 <= pivot (11) → false
  比较：arr[3] = 12 <= pivot (11) → false
  比较：arr[4] = 22 <= pivot (11) → false

  放置基准值：交换 arr[0] 和 arr[5] → [11,34,25,12,22,64,90]
  分区完成，返回分区索引：0
  ------------------------------


    快速排序调用：low=1, high=5
    分区操作开始：low=1, high=5, 基准值=pivot=64

    比较：arr[1] = 34 <= pivot (64) → true
      交换：arr[1] 和 arr[1] → [11,34,25,12,22,64,90]

    比较：arr[2] = 25 <= pivot (64) → true
      交换：arr[2] 和 arr[2] → [11,34,25,12,22,64,90]

    比较：arr[3] = 12 <= pivot (64) → true
      交换：arr[3] 和 arr[3] → [11,34,25,12,22,64,90]

    比较：arr[4] = 22 <= pivot (64) → true
      交换：arr[4] 和 arr[4] → [11,34,25,12,22,64,90]


    放置基准值：交换 arr[5] 和 arr[5] → [11,34,25,12,22,64,90]
    分区完成，返回分区索引：5
    ------------------------------


      快速排序调用：low=1, high=4
      分区操作开始：low=1, high=4, 基准值=pivot=22

      比较：arr[1] = 34 <= pivot (22) → false
      比较：arr[2] = 25 <= pivot (22) → false
      比较：arr[3] = 12 <= pivot (22) → true
        交换：arr[1] 和 arr[3] → [11,12,25,34,22,64,90]


      放置基准值：交换 arr[2] 和 arr[4] → [11,12,22,34,25,64,90]
      分区完成，返回分区索引：2
      ------------------------------


        快速排序调用：low=3, high=4
        分区操作开始：low=3, high=4, 基准值=pivot=25

        比较：arr[3] = 34 <= pivot (25) → false

        放置基准值：交换 arr[3] 和 arr[4] → [11,12,22,25,34,64,90]
        分区完成，返回分区索引：3
        ------------------------------
```